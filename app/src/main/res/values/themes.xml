<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Base app theme (Material3 + NoActionBar) -->
    <style name="Theme.ActionLens" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your app colors -->
        <item name="colorPrimary">@color/actionlens_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/actionlens_secondary</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Keep screen immersive for camera -->
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
